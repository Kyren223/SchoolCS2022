package holidays.hanukkah;

public class b2013ex3 {

    /*
    Not the given question but wanted to try it because it seemed fun
    public static int recursiveTreeMax(Tree tree) {
        if (tree == null) return -1;
        int value = tree.getValue();
        int left = recursiveTreeMax(tree.getLeft());
        int middle = recursiveTreeMax(tree.getMiddle());
        int right = recursiveTreeMax(tree.getRight());
        return Math.max(value, Math.max(left, Math.max(middle, right)));
    }
     */

}
